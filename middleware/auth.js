export default ({
  store,
  route,
  redirect,
  params,
  query,
  req,
  res,
  app:{
    $cookies
  },
}) => {
  // context服务器上下文
  // 全局守卫业务
  // store状态数信息
  // route 目标路由的信息
  // redirect 强制跳转

  // console.log("middleware")
}
